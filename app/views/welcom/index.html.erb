<h1>Hello, Rails!</h1>
<%= link_to 'My Blog', controller: 'articles' %>

<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  <br>
  <a href="#" class="oauth-signin">Sign in with Google</a>

  </li>
<% end %>
